
Moderator module

Classes
Fix RoleManagement, 

commands

intents with Online
modify channel 

Logging :

        #   Creating log channel if not exist :x:
        #ch = await GeneralModeration.CheckChannel(self, ctx)
updates

poll
warn when the user just enter one opinion
Send results after a given time to the ctx.author