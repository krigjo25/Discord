
Moderator module

Classes
Fix RoleManagement, 

commands
*   Generate a log channel if its not created yet
*   Fix itents for online and members
*   Sush not showing error with out minute
*   Remove re creation error from sush and lifted
*   crero sends start procsess on exit, remove
*   remro unkown error
*   Create a db for dnd, and back

###### Not created yet
*   moro
*   semro
modify channel
cl 

Logging :

        #   Creating log channel if not exist :x:
        #ch = await GeneralModeration.CheckChannel(self, ctx)
updates

poll
warn when the user just enter one opinion
Send results after a given time to the ctx.author